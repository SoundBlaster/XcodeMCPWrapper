# Codex CLI broker-mode MCP setup for mcpbridge-wrapper
#
# Use ONE command.
#
# OPTION 1: uvx (recommended)
# Auto-detect running broker, spawn if missing.
codex mcp add xcode -- uvx --from mcpbridge-wrapper mcpbridge-wrapper --broker
#
# OPTION 2: manual install
codex mcp add xcode -- /Users/USERNAME/bin/xcodemcpwrapper --broker
#
# OPTION 3: local dev (venv)
codex mcp add xcode -- /path/to/XcodeMCPWrapper/.venv/bin/mcpbridge-wrapper --broker
#
# Verify:
#   codex mcp list
