# Claude Code broker-mode MCP setup for mcpbridge-wrapper
#
# Use ONE command.
#
# OPTION 1: uvx (recommended)
# Auto-detect running broker, spawn if missing.
claude mcp add --transport stdio xcode -- uvx --from mcpbridge-wrapper mcpbridge-wrapper --broker
#
# OPTION 2: manual install
claude mcp add --transport stdio xcode -- /Users/USERNAME/bin/xcodemcpwrapper --broker
#
# OPTION 3: local dev (venv)
claude mcp add --transport stdio xcode -- /path/to/XcodeMCPWrapper/.venv/bin/mcpbridge-wrapper --broker
#
# Verify:
#   claude mcp list
